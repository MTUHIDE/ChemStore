@page
@model ChemStoreWebApp.Pages.Admin.PeopleInCharge.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>PersonInCharge</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="PersonInCharge.Email" class="control-label"></label>
                <input asp-for="PersonInCharge.Email" class="form-control" />
                <span asp-validation-for="PersonInCharge.Email" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="PersonInCharge.PicName" class="control-label"></label>
                <input asp-for="PersonInCharge.PicName" class="form-control" />
                <span asp-validation-for="PersonInCharge.PicName" class="text-danger"></span>
            </div>
            <input type="hidden" asp-for="PersonInCharge.PicId" />
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
