@page
@model ChemStoreWebApp.Pages.Admin.HasLocations.DetailsModel

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>HasLocation</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.HasLocation.Id)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.HasLocation.Id)
        </dd>
    </dl>
</div>
<div>
    <a asp-page="./Edit" asp-route-id="@Model.HasLocation.Id">Edit</a> |
    <a asp-page="./Index">Back to List</a>
</div>
